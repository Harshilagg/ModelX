// App-wide feature flags and config
// Set to `true` to force showing onboarding at startup (dev only)
const bool kForceShowOnboarding = false;

// When true, after signup the app will verify phone via OTP.
// Set to false to skip OTP during development.
const bool kEnablePhoneOtp = false;
